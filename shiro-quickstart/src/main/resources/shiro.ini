# Users:  username = password, role1, role2, ...
[users]
alice = secret123, admin
bob = password123, clerk

# Roles:  role = permission1, permission2, ...
[roles]
# admins can do anything with orders, including approving them
admin = order:*
# clerks may only submit orders
clerk = order:submit
